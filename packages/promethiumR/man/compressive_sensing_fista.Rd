% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recovery.R
\name{compressive_sensing_fista}
\alias{compressive_sensing_fista}
\title{Compressive Sensing via FISTA}
\usage{
compressive_sensing_fista(
  y,
  A,
  lambda = 0.1,
  max_iter = 100L,
  tolerance = 1e-05,
  verbose = FALSE
)
}
\arguments{
\item{y}{Observation vector}

\item{A}{Measurement matrix}

\item{lambda}{L1 regularization parameter}

\item{max_iter}{Maximum iterations}

\item{tolerance}{Convergence tolerance}

\item{verbose}{Print progress}
}
\value{
Recovered sparse vector
}
\description{
Solves: min_x (1/2)||Ax - y||_2^2 + lambda ||x||_1
FISTA achieves O(1/k^2) convergence rate.
}
\examples{
# Sparse signal
n <- 50
x_true <- rep(0, n)
x_true[c(5, 15, 30)] <- c(2, -1.5, 1)

# Measurement
m <- 30
A <- matrix(rnorm(m * n), m, n)
y <- A \%*\% x_true + rnorm(m) * 0.01

# Recover
x_rec <- compressive_sensing_fista(y, A, lambda = 0.1)

}
