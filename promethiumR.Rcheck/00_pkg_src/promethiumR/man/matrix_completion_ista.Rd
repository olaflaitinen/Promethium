% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recovery.R
\name{matrix_completion_ista}
\alias{matrix_completion_ista}
\title{Matrix Completion via ISTA}
\usage{
matrix_completion_ista(
  observed,
  mask,
  lambda = 0.1,
  max_iter = 100L,
  tolerance = 1e-05,
  verbose = FALSE
)
}
\arguments{
\item{observed}{Numeric matrix (missing entries can be any value)}

\item{mask}{Logical matrix (TRUE = observed, FALSE = missing)}

\item{lambda}{Regularization parameter for nuclear norm}

\item{max_iter}{Maximum number of iterations}

\item{tolerance}{Convergence tolerance}

\item{verbose}{Logical, print progress}
}
\value{
Completed numeric matrix
}
\description{
Solves: min_X (1/2)||P_Omega(X - M)||\emph{F^2 + lambda ||X||}*
where ||X||_* is the nuclear norm.
}
\examples{
# Create low-rank matrix
U <- matrix(rnorm(20 * 3), 20, 3)
V <- matrix(rnorm(20 * 3), 20, 3)
M <- U \%*\% t(V)

# Create mask (60\% observed)
mask <- matrix(runif(400) < 0.6, 20, 20)
observed <- M * mask

# Complete matrix
completed <- matrix_completion_ista(observed, mask, lambda = 0.1)

}
